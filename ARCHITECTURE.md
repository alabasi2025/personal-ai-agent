# 🧠 وكيلك الشخصي - Personal AI Agent

## نظرة عامة

وكيل ذكي يعمل على جهازك المحلي، يجمع كل أدواتك في مكان واحد مع ذاكرة دائمة.

---

## 🏗️ البنية الأساسية

```
┌─────────────────────────────────────────────────────────────┐
│                    واجهة المستخدم                           │
│              (Web UI / CLI / API)                           │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                   🧠 العقل المركزي                          │
│                   (Core Brain)                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  • تحليل الطلبات                                    │   │
│  │  • اتخاذ القرارات                                   │   │
│  │  • تنسيق المهام                                     │   │
│  │  • التعلم من التفاعلات                              │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                   💾 الذاكرة الدائمة                        │
│                   (Persistent Memory)                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  • المحادثات (conversations)                        │   │
│  │  • المشاريع (projects)                              │   │
│  │  • التفضيلات (preferences)                          │   │
│  │  • المعرفة (knowledge)                              │   │
│  │  • السياق (context)                                 │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                   🔌 موصلات الأدوات                         │
│                   (Tool Connectors)                         │
│  ┌─────────────┬─────────────┬─────────────────────────┐   │
│  │   Manus     │   Cursor    │     Google AI           │   │
│  │  Connector  │  Connector  │     Connector           │   │
│  │             │             │                         │   │
│  │  • تنفيذ   │  • كود     │  • Gemini API          │   │
│  │  • ملفات  │  • تعديل   │  • Antigravity         │   │
│  │  • متصفح  │  • مشاريع  │  • NotebookLM          │   │
│  └─────────────┴─────────────┴─────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 هيكل الملفات

```
personal-ai-agent/
├── server/                    # Backend
│   ├── index.ts              # نقطة الدخول
│   ├── brain/                # العقل المركزي
│   │   ├── analyzer.ts       # تحليل الطلبات
│   │   ├── router.ts         # توجيه المهام
│   │   └── learner.ts        # التعلم
│   ├── memory/               # الذاكرة
│   │   ├── store.ts          # تخزين
│   │   ├── retriever.ts      # استرجاع
│   │   └── embeddings.ts     # تضمينات
│   ├── connectors/           # موصلات
│   │   ├── manus.ts          # Manus API
│   │   ├── cursor.ts         # Cursor API
│   │   └── google.ts         # Google AI API
│   └── api/                  # REST API
│       └── routes.ts
├── client/                   # Frontend
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   └── pages/
│   └── index.html
├── database/                 # قاعدة البيانات
│   └── schema.sql
├── config/                   # الإعدادات
│   └── settings.json
└── package.json
```

---

## 💾 قاعدة البيانات (SQLite)

### جدول المحادثات (conversations)
```sql
CREATE TABLE conversations (
    id TEXT PRIMARY KEY,
    title TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    metadata JSON
);
```

### جدول الرسائل (messages)
```sql
CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    conversation_id TEXT REFERENCES conversations(id),
    role TEXT CHECK(role IN ('user', 'assistant', 'system')),
    content TEXT,
    tool_used TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    embeddings BLOB
);
```

### جدول المشاريع (projects)
```sql
CREATE TABLE projects (
    id TEXT PRIMARY KEY,
    name TEXT,
    path TEXT,
    description TEXT,
    tech_stack JSON,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### جدول التفضيلات (preferences)
```sql
CREATE TABLE preferences (
    key TEXT PRIMARY KEY,
    value JSON,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### جدول المعرفة (knowledge)
```sql
CREATE TABLE knowledge (
    id TEXT PRIMARY KEY,
    category TEXT,
    content TEXT,
    source TEXT,
    embeddings BLOB,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔌 الموصلات (Connectors)

### 1. Manus Connector
```typescript
interface ManusConnector {
    // تنفيذ أوامر
    executeCommand(command: string): Promise<Result>;
    // إدارة ملفات
    readFile(path: string): Promise<string>;
    writeFile(path: string, content: string): Promise<void>;
    // تصفح
    browse(url: string): Promise<PageContent>;
}
```

### 2. Cursor Connector
```typescript
interface CursorConnector {
    // فتح مشروع
    openProject(path: string): Promise<void>;
    // تعديل ملف
    editFile(path: string, instructions: string): Promise<void>;
    // إنشاء ملف
    createFile(path: string, content: string): Promise<void>;
    // تشغيل Agent
    runAgent(task: string): Promise<Result>;
}
```

### 3. Google AI Connector
```typescript
interface GoogleAIConnector {
    // Gemini API
    chat(messages: Message[]): Promise<Response>;
    // تحليل مستندات
    analyzeDocument(file: string): Promise<Analysis>;
    // بحث
    search(query: string): Promise<SearchResults>;
}
```

---

## 🧠 موزع المهام الذكي

```typescript
class TaskRouter {
    async route(task: string): Promise<Tool> {
        // تحليل نوع المهمة
        const analysis = await this.analyze(task);
        
        // اختيار الأداة المناسبة
        if (analysis.needsExecution) return 'manus';
        if (analysis.needsCoding) return 'cursor';
        if (analysis.needsAnalysis) return 'google';
        if (analysis.needsResearch) return 'google';
        
        // افتراضي
        return 'google';
    }
    
    private async analyze(task: string): Promise<TaskAnalysis> {
        // استخدام Gemini لتحليل المهمة
        const result = await this.googleAI.analyze(task);
        return result;
    }
}
```

---

## 🎯 سيناريوهات الاستخدام

### 1. طلب برمجة
```
المستخدم: "أنشئ لي API للمستخدمين"
    ↓
العقل: يحلل → مهمة برمجة
    ↓
الموجه: يختار → Cursor
    ↓
Cursor: ينفذ → ينشئ الملفات
    ↓
الذاكرة: تحفظ → المشروع + الكود
```

### 2. طلب تنفيذ
```
المستخدم: "شغّل الخادم على المنفذ 3000"
    ↓
العقل: يحلل → مهمة تنفيذ
    ↓
الموجه: يختار → Manus
    ↓
Manus: ينفذ → يشغل الأمر
    ↓
الذاكرة: تحفظ → النتيجة
```

### 3. طلب بحث/تحليل
```
المستخدم: "ابحث عن أفضل طريقة لـ..."
    ↓
العقل: يحلل → مهمة بحث
    ↓
الموجه: يختار → Google AI
    ↓
Gemini: يبحث → يحلل → يلخص
    ↓
الذاكرة: تحفظ → المعرفة الجديدة
```

---

## 🔐 الأمان

- كل البيانات محلية على جهازك
- مفاتيح API مشفرة
- لا إرسال بيانات لأطراف ثالثة
- تشفير قاعدة البيانات (اختياري)

---

## 📦 التقنيات المستخدمة

| المكون | التقنية |
|--------|---------|
| **Backend** | Node.js + TypeScript |
| **Frontend** | React + TailwindCSS |
| **Database** | SQLite |
| **AI** | Gemini API + OpenAI API |
| **Embeddings** | sentence-transformers |

---

## 🚀 خطة التنفيذ

1. ✅ تصميم البنية
2. ⏳ بناء الذاكرة الدائمة
3. ⏳ بناء الموصلات
4. ⏳ بناء موزع المهام
5. ⏳ بناء الواجهة
6. ⏳ الاختبار والنشر
